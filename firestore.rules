rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Каждый пользователь видит и пишет только свои данные.
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      match /maps/{mapId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
